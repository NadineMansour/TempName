<%= image_tag(@user.profileImage) %>

<!-- creayte new post - edited_by mariam -->
<% if auth_user_profile?(1) %>
	<%= form_for @post, :url => posts_path, :remote=>true  do |p| %>
		<%= p.label :title %>
		<%= p.text_field :title %>
		<br>
		<%= p.label :body %>
		<%= p.text_area :body %>
		<br>
		<%= social_share_button_tag("My Site") %>
		<p>Related to : </p>
		<br>
		<%= collection_check_boxes(:post, :related_categories, Category.all, :id, :name) %> 
		<br>
		<%= p.submit "Create" %>
	<% end %>
<% end %>
<!-- end -->

<%= link_to "Edit info", '/user/edit_info' %>