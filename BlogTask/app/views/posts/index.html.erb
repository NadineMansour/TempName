<% @posts.each do |post| %>
	<pre>Title:
	<%= post[:title] %>
	</pre>
	<br>
	<pre>Body:
	<%= post[:body] %>
	</pre>
	<br>
	<% @temp_post = Post.find(post[:id]) %>
	<% @att = @temp_post.post_attachments.all %>
	<% @att.each do |p| %>
	  <%= image_tag p.avatar_url %>
	<% end %>
<% end %>